@model MonPDReborn.Models.CCTVParkir.MonitoringCCTVVM.Kapasitas


@{
    ViewData["Title"] = "Jumlah Terparkir";
    Layout = "_Layout";
}
<div class="row">

    <div class="col-lg-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center bg-header-color">
                <h4 class="card-title text-dark mb-0">Monitoring CCTV Parkir</h4>
            </div>


            <div class="card-body">

                <div class="row text-left g-3 mt-1">
                    <!-- Col 1 -->
                    <div class="col-md-6">
                        <h5 class="text-dark">Nama OP : Static Nama Op</h5>
                        <h5 class="text-dark">No OP : Static No OP</h5>
                        <h5 class="text-dark">Alamat : static Lokasi</h5>
                    </div>

                    <!-- Col 2 -->
                    <div class="col-md-6">
                        <h5 class="text-dark">Vendor : static vendor</h5>
                        <h5 class="text-dark">Tanggal Pasang : static tanggal</h5>

                    </div>
                </div>

                <div class="row g-3 align-items-center">


                    <div class="table-responsive">
                        @(
                                                Html.DevExtreme().DataGrid<object>() // pakai object dulu kalau model belum ada
                                                .ID("KapasitasMonitoringCCTVGrid")
                                                .DataSource(new object[] { }) // data kosong
                                                .ShowBorders(true)
                                                .ShowColumnHeaders(true)
                                                .ShowColumnLines(true)
                                                .ShowRowLines(true)
                                                .AllowColumnResizing(true)
                                                .RowAlternationEnabled(true)
                                                .ColumnAutoWidth(true)
                                                .SearchPanel(s => s.Visible(true).HighlightCaseSensitive(false))
                                                .FilterRow(f => f.Visible(true))
                                                .HeaderFilter(h => h.Visible(true))
                                                .Sorting(s => s.Mode(GridSortingMode.Multiple))
                                                .Paging(p => p.PageSize(10))
                                                .Pager(p => p
                                                .ShowPageSizeSelector(true)
                                                .AllowedPageSizes(new[] { 10, 20, 50 })
                                                .ShowInfo(true)
                                                )
                                                .Export(e => e.Enabled(true).AllowExportSelectedData(false).Formats(new[] { "xlsx", "pdf" }))
                                                .OnExporting("exportGrid")
                                                .Columns(columns =>
                                                {
                                                    columns.Add().Caption("Bulan");
                                                    columns.Add().Caption("Motor");
                                                    columns.Add().Caption("Mobil");
                                                    columns.Add().Caption("Uknown");
                                                    columns.Add().Caption("Omset");
                                                    columns.Add().Caption("Est. Pajak");
                                                    columns.Add().Caption("Tahun Kemaren");
                                                    columns.Add().Caption("Tahun ini");
                                                    columns.Add().Caption("Potensi");
                                                })
                                                )
                    </div>


                    @* <div class="col-lg col-md-6">
                        @(Html.DevExtreme().DateBox()
                            .ID("bulanAwal")
                            .Type(DateBoxType.Date)
                            .Placeholder("Bulan Awal")
                            .StylingMode(EditorStylingMode.Outlined)
                            .Value(DateTime.Now.AddMonths(-1))
                            .DisplayFormat("MMMM yyyy")
                            .CalendarOptions(c => c.MaxZoomLevel(CalendarZoomLevel.Year))
                            )
                    </div>

                    <div class="col-lg col-md-6">
                        @(Html.DevExtreme().DateBox()
                            .ID("bulanAkhir")
                            .Type(DateBoxType.Date)
                            .Placeholder("Bulan Akhir")
                            .StylingMode(EditorStylingMode.Outlined)
                            .Value(DateTime.Now)
                            .DisplayFormat("MMMM yyyy")
                            .CalendarOptions(c => c.MaxZoomLevel(CalendarZoomLevel.Year))
                            )
                    </div>


                    <div class="col-lg col-md-6">
                        <button type="button" onclick="fnKapasitasParkir()" class="btn btn-success w-100">
                            Terapkan Filter
                        </button>
                    </div> *@





                    @* <div class="text-center">
                        <div id="loading2-DataKapasitasParkir" class="spinner-border text-primary" style="display: none">
                            <span class="visually-hidden">Loading...</span>
                        </div>
                    </div>
                    <div id="DataKapasitasParkir" class="mt-3"></div> *@

                    

                </div>

                <div class="table-responsive mt-4">
                    @(
                                        Html.DevExtreme().DataGrid<object>()
                                        .ID("KapasitasMonitoringCCTVHarianGrid")
                                        .DataSource(new object[] { }) // kosong dulu
                                        .ShowBorders(true)
                                        .ShowColumnHeaders(true)
                                        .RowAlternationEnabled(true)
                                        .ColumnAutoWidth(true)
                                        .Paging(p => p.PageSize(10))
                                        .Columns(columns =>
                                        {
                                            columns.Add().Caption("Tanggal");
                                            columns.Add().Caption("Motor");
                                            columns.Add().Caption("Mobil");
                                            columns.Add().Caption("Unknown");
                                            columns.Add().Caption("Omset");
                                            columns.Add().Caption("Est. Pajak");
                                        })
                                        )
                </div>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(function () {
            // Dummy data bulanan
            var dummyBulanan = [
                { Bulan: "Januari", Motor: 120, Mobil: 80, Unknown: 5, Omset: 2000000, EstPajak: 500000, TahunKemaren: 150, TahunIni: 205, Potensi: 3000000 },
                { Bulan: "Februari", Motor: 90, Mobil: 70, Unknown: 2, Omset: 1500000, EstPajak: 375000, TahunKemaren: 120, TahunIni: 162, Potensi: 2800000 },
                { Bulan: "Maret", Motor: 110, Mobil: 85, Unknown: 3, Omset: 2100000, EstPajak: 525000, TahunKemaren: 160, TahunIni: 198, Potensi: 3100000 }
            ];

            $("#KapasitasMonitoringCCTVGrid").dxDataGrid("instance").option("dataSource", dummyBulanan);
        });

        // Dummy data harian
            var dummyHarian = [
                { Tanggal: "2025-09-01", Motor: 15, Mobil: 8, Unknown: 0, Omset: 400000, EstPajak: 100000 },
                { Tanggal: "2025-09-02", Motor: 12, Mobil: 10, Unknown: 1, Omset: 380000, EstPajak: 95000 },
                { Tanggal: "2025-09-03", Motor: 20, Mobil: 7, Unknown: 0, Omset: 500000, EstPajak: 125000 }
            ];
            $("#KapasitasMonitoringCCTVHarianGrid").dxDataGrid("instance").option("dataSource", dummyHarian);

            // Event klik baris bulanan → filter harian
            $("#KapasitasMonitoringCCTVGrid").dxDataGrid("instance").option("onRowClick", function (e) {
                var selectedMonth = e.data.Bulan;
                var filtered = dummyHarian.filter(x => x.Tanggal.startsWith("2025-09")); // sementara hardcode
                $("#KapasitasMonitoringCCTVHarianGrid").dxDataGrid("instance").option("dataSource", filtered);
            });
        });
    </script>


}


@section Scripts {
    <script>
        $(document).ready(function () {
            fnKapasitasParkir();
        });

        function fnKapasitasParkir() {

            var bulanAwal = $("#bulanAwal").dxDateBox("instance").option("value");
            var bulanAkhir = $("#bulanAkhir").dxDateBox("instance").option("value");

            if (!bulanAwal || !bulanAkhir) {
                alert("Harap pilih kedua bulan.");
                return;
            }

            var startDate = new Date(bulanAwal.getFullYear(), bulanAwal.getMonth(), 1);
            var endDate = new Date(bulanAkhir.getFullYear(), bulanAkhir.getMonth() + 1, 0);

            if (startDate > endDate) {
                alert("Bulan Awal tidak boleh lebih besar dari Bulan Akhir.");
                return;
            }

            $("#DataKapasitasParkir").html(
                '<div class="text-center"><div class="spinner-border text-primary" role="status"><span class="visually-hidden">Loading...</span></div></div>'
            );

            $.ajax({
                url: '@Url.Action("DataKapasitasParkir")',
                type: 'GET',
                data: {
                    nop: '@(Model.Nop)',
                    tanggalAwal: startDate.toISOString().split('T')[0], // yyyy-MM-dd
                    tanggalAkhir: endDate.toISOString().split('T')[0]
                },
                beforeSend: function () {
                    $('#loading2-DataKapasitasParkir').show();
                    $('#DataKapasitasParkir').hide();
                },
                success: function (result) {
                    $('#loading2-DataKapasitasParkir').hide();
                    $('#DataKapasitasParkir').html(result).show();
                },
                error: function () {
                    $('#loading2-DataKapasitasParkir').hide();
                    $('#DataKapasitasParkir').html('<div class="alert alert-danger">❌ Gagal memuat data.</div>').show();
                }
            });
        }

        function lihatDetailLog(nop, jenisKend) {
            var bulanAwal = $("#bulanAwal").dxDateBox("instance").option("value");
            var bulanAkhir = $("#bulanAkhir").dxDateBox("instance").option("value");

            if (!bulanAwal || !bulanAkhir) {
                alert("Harap pilih kedua bulan.");
                return;
            }

            var startDate = new Date(bulanAwal.getFullYear(), bulanAwal.getMonth(), 1);
            var endDate = new Date(bulanAkhir.getFullYear(), bulanAkhir.getMonth() + 1, 0);

            if (startDate > endDate) {
                alert("Bulan Awal tidak boleh lebih besar dari Bulan Akhir.");
                return;
            }

            // Tampilkan modal
            $("#staticBackdropLargeScrollable").modal("show");

            // Kosongkan / tampilkan loading spinner saat request
            $("#staticBackdropLargeScrollable .modal-body").html(`
                <div class="text-center p-3">
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                </div>
            `);

            // Ambil konten detail via AJAX
            $.get('/MonitoringCCTV/Log', { nop: nop, jenisKend : jenisKend,tanggalAwal: startDate.toISOString().split('T')[0], // yyyy-MM-dd
                    tanggalAkhir: endDate.toISOString().split('T')[0]  })
                .done(function (html) {
                    $("#staticBackdropLargeScrollable .modal-title").html("Detail Kendaraan");
                    $("#staticBackdropLargeScrollable .modal-body").html(html);
                })
                .fail(function () {
                    $("#staticBackdropLargeScrollable .modal-body").html(
                        `<div class="text-danger text-center p-3">
                            Gagal memuat data detail.
                        </div>`
                    );
                });
        }
    </script>
}