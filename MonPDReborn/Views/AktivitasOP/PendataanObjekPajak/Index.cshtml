@using MonPDReborn.Models.AktivitasOP
@model PendataanObjekPajakVM.Index

@{
    ViewData["Title"] = "Pendataan Objek";
    Layout = "_Layout";
}

<div class="row">
    <!-- Dashboard Resto -->
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header d-flex">
                <div class="flex-grow-1">
                    <h4 class="card-title fs-16 mb-0 text-primary">
                        Dashboard Pengedokan Pajak - Resto
                    </h4>
                </div>
            </div>
        </div>
    </div>

    <div class="col-lg-3">
        <div class="card shadow-sm rounded-xl p-3 text-center" style="height:75%;">
            <div class="card-body">
                <div class="d-flex justify-content-start align-items-center mb-2">
                    <div class="rounded-circle bg-primary-subtle p-2 me-2">
                        <i class="ri-search-eye-line text-primary fs-1"></i>
                    </div>
                    <div class="text-start">
                        <h5 class="text-muted">Potensi Pajak Hasil Pengedokan</h5>
                        <h4 class="mb-0 fw-bold">Rp @Model.Data.TotalPengedokanResto.ToString("N0")</h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-3">
        <div class="card shadow-sm rounded-xl p-3 text-center" style="height:75%;">
            <div class="card-body">
                <div class="d-flex justify-content-start align-items-center mb-2">
                    <div class="rounded-circle bg-primary-subtle p-2 me-2">
                        <i class="ri-team-fill text-primary fs-1"></i>
                    </div>
                    <div class="text-start">
                        <h5 class="text-muted">Total Objek</h5>
                        <h4 class="mb-0 fw-bold">@Model.Data.JumlahObjekResto.ToString("N0")</h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-3">
        <div class="card shadow-sm rounded-xl p-3 text-center" style="height:75%;">
            <div class="card-body">
                <div class="d-flex justify-content-start align-items-center mb-2">
                    <div class="rounded-circle bg-primary-subtle p-2 me-2">
                        <i class="ri-bar-chart-box-line text-primary fs-1"></i>
                    </div>
                    <div class="text-start">
                        <h5 class="text-muted">Rata-Rata Realisasi</h5>
                        <h4 class="mb-0 fw-bold">Rp @Model.Data.RatarataResto.ToString("N0")</h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-3">
        <div class="card shadow-sm rounded-xl p-3 text-center" style="height:75%;">
            <div class="card-body">
                <div class="d-flex justify-content-start align-items-center mb-2">
                    <div class="rounded-circle bg-primary-subtle p-2 me-2">
                        <i class="ri-money-dollar-box-line text-primary fs-1"></i>
                    </div>
                    <div class="text-start">
                        <h5 class="text-muted">Total Realisasi</h5>
                        <h4 class="mb-0 fw-bold">Rp @Model.Data.TotalRealisasiResto.ToString("N0")</h4>
                    </div>
                </div>
            </div>
        </div>
    </div>


    <!-- Dashboard Parkir -->
    <div class="col-lg-12 mt-4">
        <div class="card">
            <div class="card-header d-flex">
                <div class="flex-grow-1">
                    <h4 class="card-title fs-16 mb-0 text-success">
                        Dashboard Pengedokan Pajak - Parkir
                    </h4>
                </div>
            </div>
        </div>
    </div>

    <div class="col-lg-3">
        <div class="card shadow-sm rounded-xl p-3 text-center" style="height:75%;">
            <div class="card-body">
                <div class="d-flex justify-content-start align-items-center mb-2">
                    <div class="rounded-circle bg-success-subtle p-2 me-2">
                        <i class="ri-parking-box-line text-success fs-1"></i>
                    </div>
                    <div class="text-start">
                        <h5 class="text-muted">Potensi Pajak Hasil Pengedokan</h5>
                        <h4 class="mb-0 fw-bold">Rp @Model.Data.TotalPengedokanParkir.ToString("N0")</h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-3">
        <div class="card shadow-sm rounded-xl p-3 text-center" style="height:75%;">
            <div class="card-body">
                <div class="d-flex justify-content-start align-items-center mb-2">
                    <div class="rounded-circle bg-success-subtle p-2 me-2">
                        <i class="ri-team-line text-success fs-1"></i>
                    </div>
                    <div class="text-start">
                        <h5 class="text-muted">Total Objek</h5>
                        <h4 class="mb-0 fw-bold">@Model.Data.JumlahObjekParkir.ToString("N0")</h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-3">
        <div class="card shadow-sm rounded-xl p-3 text-center" style="height:75%;">
            <div class="card-body">
                <div class="d-flex justify-content-start align-items-center mb-2">
                    <div class="rounded-circle bg-success-subtle p-2 me-2">
                        <i class="ri-bar-chart-grouped-line text-success fs-1"></i>
                    </div>
                    <div class="text-start">
                        <h5 class="text-muted">Rata-Rata Realisasi</h5>
                        <h4 class="mb-0 fw-bold">Rp @Model.Data.RatarataParkir.ToString("N0")</h4>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-3">
        <div class="card shadow-sm rounded-xl p-3 text-center" style="height:75%;">
            <div class="card-body">
                <div class="d-flex justify-content-start align-items-center mb-2">
                    <div class="rounded-circle bg-success-subtle p-2 me-2">
                        <i class="ri-money-cny-circle-line text-success fs-1"></i>
                    </div>
                    <div class="text-start">
                        <h5 class="text-muted">Total Realisasi</h5>
                        <h4 class="mb-0 fw-bold">Rp @Model.Data.TotalRealisasiParkir.ToString("N0")</h4>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-lg-12">
        <div class="card">
            <div class="card-header d-flex bg-header-color align-items-center">
                <div class="flex-grow-1">
                    <h4 class="card-title fs-16 text-dark mb-0">
                        Pengedokan Pajak
                    </h4>
                </div>
                <div class="flex-shrink-0 text-end">
                    <button class="btn btn-sm btn-outline-dark" title="Segarkan data realisasi pajak" onclick="fnSeriesPajakDaerah()">
                        <i class="mdi mdi-refresh align-middle"></i> Refresh
                    </button>
                </div>
            </div>
            <div class="card-body">
                <div class="text-center">
                    <div id="loading2-Show" class="spinner-border text-primary" style="display: none">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                </div>
                <div id="Show"></div>
            </div>
        </div>
    </div>

    <div class="col-lg-12" id="detailWrapper" style="display: none;">
        <div class="card shadow-sm">
            <div class="card-header d-flex bg-header-color align-items-center">
                <div class="flex-grow-1">
                    <h4 class="card-title text-dark fs-16 mb-0">
                        Detail Pengedokan Pajak
                    </h4>
                </div>
                <div class="flex-shrink-0 text-end">
                    <button class="btn btn-sm btn-outline-dark" title="Segarkan data realisasi pajak" onclick="fnSeriesPajakDaerah()">
                        <i class="mdi mdi-refresh align-middle"></i> Refresh
                    </button>
                    <button onclick="hideDetail()" class="btn btn-outline-dark btn-sm">
                        <i class="ri-close-fill align-middle"></i>
                        Tutup
                    </button>
                </div>
            </div>
            <div class="card-body">
                <div id="Detail"></div>
                <div class="modal fade" id="detailModal" tabindex="-1">
                    <div class="modal-dialog modal-xl">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">Detail</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                            </div>
                            <div class="modal-body">
                                <div class="text-center py-5">
                                    <div class="spinner-border text-primary"></div>
                                    <p>Loading data...</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>


</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            loadShow(); // Load data rekap
        });

        // Muat partial _Show.cshtml
        function loadShow() {
            $.ajax({
                url: '@Url.Action("Show", "PendataanObjekPajak")',
                type: 'GET',
                beforeSend: function () {
                    $('#loading2-Show').show();
                    $('#Show').hide();
                },
                success: function (result) {
                    $('#loading2-Show').hide();
                    $('#Show').html(result).show();
                },
                error: function () {
                    $('#loading2-Show').hide();
                    $('#Show').html(`
                        <div class="alert alert-danger">
                            ❌ Gagal memuat data rekap.
                        </div>
                    `).show();
                }
            });
        }


        // Muat partial _Detail.cshtml berdasarkan jenis pajak dan tahun
        function loadDetail(jenisPajak, tahun) {
            $('#Detail').html('<div class="text-center p-3">Memuat data detail...</div>');
            $('#detailWrapper').slideDown(300); // pastikan wrapper tampil

            $.ajax({
                url: '@Url.Action("Detail", "PendataanObjekPajak")',
                type: 'GET',
                data: {
                    jenisPajak: jenisPajak,
                    tahun, tahun
                },
                success: function (result) {
                    $('#Detail').html(result);
                },
                error: function () {
                    $('#Detail').html('<div class="alert alert-danger">Gagal memuat data detail.</div>');
                }
            });
        }

        function hideDetail() {
            $('#detailWrapper').slideUp(300);
        }

        // Contoh: bisa digunakan jika ingin memfilter via dropdown
        function onJenisPajakChanged(jenis, tahun) {
            loadDetail(jenis, tahun);
        }

         function onDetailClick(e) {
            e.event.preventDefault();
            const data = e.row.data;

            console.log("DATA CLIENT:", data);

            if (!data) {
                DevExpress.ui.notify("Data tidak ditemukan", "error", 2000);
                return;
            }

            const jenisPajak = data.EnumPajak;
            const nop = data.NOP;
            const tahun = data.Tahun;


            const $modal = $("#staticBackdropExtraLargeScrollable");
            $modal.find(".modal-title").text(`Detail - ${nop}`);
            $modal.find(".modal-body").html(`
                <div class="text-center py-5">  
                    <div class="spinner-border text-primary"></div>
                    <p>Loading data...</p>
                </div>
            `);

            $modal.modal("show");

            $.ajax({
                url: '/PendataanObjekPajak/SubDetail',
                type: 'GET',
                data: { jenisPajak, nop, tahun },
                success: function (response) {
                    $modal.find(".modal-body").html(response);
                },
                error: function () {
                    $modal.find(".modal-body").html(`
                        <div class="alert alert-danger">Gagal memuat data.</div>
                    `);
                }
            });
        }

    </script>
}
